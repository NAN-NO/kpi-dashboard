window.addEventListener('DOMContentLoaded', () => {
        // Step 1: load data from localStorage (or defaults)
        loadInitialData();

        // Step 2: auto-detect เดือนล่าสุดที่มีข้อมูล
        autoSetLatestMonth();

        // Step 3: populate filters & selectors
        initFilters();
        updateKpiSelector();

        // Step 4: render overview + detail sections
        calculateOverviewDashboard();
        handleKpiChange();
        updatePeriodLabel();

        // Step 5: attempt auto-load from Google Sheet
        setTimeout(() => loadFromSheet(true), 800);
    });